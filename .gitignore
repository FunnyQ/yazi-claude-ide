.DS_Store
.handoffs/
.cockpit/
/target

# Local development material: the plan, the measurement docs, the spikes, and
# the manual harness. Present in a working copy, never published.
dev/

# Flightplan/autopilot working material: plans, task trees, eval rubrics, and
# the inlined contract copy the executors read. Scaffolding, not product.
docs/
